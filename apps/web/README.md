# @billow/web

Next.js App Router package for Billow.

Run common commands from the repository root through Turborepo:

```bash
npm run dev:local
npm run lint
npm run test:run
npm run build
```

Package-local commands still work from this directory when needed.
